import React from 'react';

const AddWord = () => {
  return (
    <form>
      <input
        type="text"
        placeholder="Add word here">
      </input>
      <button>Add Word</button>
    </form>
  );
};

export default AddWord;